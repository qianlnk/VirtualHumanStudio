server {
    listen 80;
    server_name backend.aivhs.cn;

    # 增加客户端请求体大小限制
    client_max_body_size 100M;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}